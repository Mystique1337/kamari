"""Kámárí data pipeline package: registry, manifest schema, adapters, and utils.

Importable from notebooks via:

    import sys, pathlib
    sys.path.append(str(pathlib.Path.cwd().parent))  # repo root
    from data.manifest_schema import MANIFEST_COLUMNS, empty_manifest, validate
"""
