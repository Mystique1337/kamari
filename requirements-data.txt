# Kámárí data pipeline (notebooks 01–04) — local / Colab
# Install with: pip install -r requirements-data.txt
numpy>=1.26
pandas>=2.2
pyarrow>=15.0
Pillow>=10.2
opencv-python-headless>=4.9
scikit-image>=0.22
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13
tqdm>=4.66
PyYAML>=6.0
python-dotenv>=1.0
gdown>=5.2
scipy>=1.11
tabulate>=0.9
# Face detection / alignment (MTCNN). Torch is pulled in for the detector.
facenet-pytorch>=2.5.3
torch>=2.2
# Dataset access + artifact storage
huggingface_hub>=0.23
datasets>=2.19
kaggle>=1.6
# Optional experiment tracking (enabled only if WANDB_API_KEY is set)
wandb>=0.17
